import SwiftUI

struct SettingsView: View {
    var body: some View {
        VStack {
            Text("Настройки (заглушка)")
                .font(.title)
                .padding()
            Spacer()
        }
    }
}

#Preview {
    SettingsView()
} 