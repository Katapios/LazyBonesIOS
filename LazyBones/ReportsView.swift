import SwiftUI

struct ReportsView: View {
    var body: some View {
        VStack {
            Text("Отчёты (заглушка)")
                .font(.title)
                .padding()
            Spacer()
        }
    }
}

#Preview {
    ReportsView()
} 