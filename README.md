# 📱 LazyBones — ежедневные отчёты с Telegram

LazyBones — iOS‑приложение для ведения ежедневных отчётов о продуктивности. Поддерживает статусы выполнения дня, напоминания, отправку отчётов в Telegram (вручную и автоматически), виджеты и удобное редактирование.

## ✨ Ключевые возможности

- __Типы отчётов__ (см. `LazyBones/Domain/Entities/DomainPost.swift → PostType`):
  - Regular — ежедневный локальный отчёт
  - Custom — произвольный локальный отчёт (поддерживает оценку)
  - External — внешний отчёт из Telegram (только просмотр)
  - iCloud — отчёт, импортированный из iCloud (только просмотр)
- __Структура локального отчёта__ (Regular/Custom):
  - Секции «Хорошее»/«Плохое»
  - Голосовые заметки (запись/воспроизведение) — см. `VoiceRecorderTests`
  - Теги (локальные), быстрый выбор и автодополнение
- __Статусы дня__ (см. [STATUS_MODEL_README.md](STATUS_MODEL_README.md)):
  - `.notStarted`, `.inProgress`, `.sent`, `.notCreated`, `.notSent`
  - Однократная разблокировка редактирования через `forceUnlock`
- __Напоминания__:
  - Режим уведомлений: ежечасно или два раза в день — `NotificationManagerService`
  - Учёт активного периода дня (начало/конец), синхронизация с таймером — `ReportStatusManager`, `PostTimerService`
- __Telegram__:
  - Ручная отправка локальных отчётов (Regular/Custom)
  - Автоотправка локальных отчётов по расписанию
  - Быстрая перенастройка токена/чата, обработка типовых ошибок
- __Виджет__ (`LazyBonesWidget`):
  - Отображение статуса отчёта и быстрых действий
  - Обновление через `WidgetCenter.reloadAllTimelines()` при изменениях
- __Хранение данных__:
  - Локально через `LocalReportService`
  - Поддержка изоляции при тестировании (in-memory стора)
- __Экспорт/iCloud__:
  - Экспорт состояний и интеграция с iCloud (см. `ICloudServiceTests` и соответствующие сервисы)

## 🧭 Навигация по документации

- Настройка Telegram: [TELEGRAM_SETUP.md](TELEGRAM_SETUP.md)
- Статусная модель: [STATUS_MODEL_README.md](STATUS_MODEL_README.md)
- Руководство по тестам: [TESTING_GUIDE.md](TESTING_GUIDE.md)
- Миграция на Clean Architecture (текущий статус): [CLEAN_ARCHITECTURE_MIGRATION.md](CLEAN_ARCHITECTURE_MIGRATION.md)

## 📚 Полный каталог функций

- __Главный экран__ (`LazyBones/Views/MainView.swift`, `MainViewNew.swift`):
  - Статус дня и таймер («До старта/До конца») — `MainStatusBarView`
  - Быстрые действия: создать/редактировать отчёт, отправить в Telegram, открыть настройки

- __Отчёты__:
  - Regular: ежедневный локальный отчёт за текущий день
  - Custom: произвольные локальные отчёты, отдельные карточки — `ReportCardView`, `ReportCardViewNew` (поддерживают оценку)
  - External/iCloud: только просмотр (не редактируются и не отправляются)
  - Формы локальных отчётов — `Views/Forms/RegularReportFormView.swift`, `PostFormView.swift`
  - Сохранение черновика и публикация локальных — `PostFormViewModel.saveAndNotify()/publishAndNotify()`

- __Секции «Хорошее/Плохое»__:
  - Добавление, удаление, сортировка пунктов; фильтрация пустых — `PostFormViewModel`
  - Быстрые пресеты и оценка состояния — `CustomReportEvaluationViewNew`

- __Планирование дня__ — `Views/Forms/DailyPlanningFormView.swift`, `DailyReportView.swift`

- __Голосовые заметки__ — `Views/VoiceRecorderView.swift`
  - Запись, воспроизведение, прикрепление к отчёту
  - Тесты: `Tests/VoiceRecorderTests.swift`

- __Теги__ — `Views/Forms/TagManagerView.swift`
  - Локальные теги, быстрый выбор, создание/удаление
  - Сервисы/хранилище: `LocalReportService` (теги, миграция старых тегов)

- __Статусная модель__ — `Core/Services/ReportStatusManager.swift`
  - Статусы: `.notStarted`, `.inProgress`, `.sent`, `.notCreated`, `.notSent`
  - `forceUnlock` для разовой разблокировки редактирования
  - Нотификация `Notification.Name.reportStatusDidChange`
  - Подробно: [STATUS_MODEL_README.md](STATUS_MODEL_README.md)

- __Уведомления__ — `Core/Services/NotificationManagerService.swift`
  - Планирование локальных уведомлений по часам/интервалу
  - Перепланирование при смене статуса и начале/конце периода
  - Учет разрешений, превью уведомлений, отключение побочных эффектов под XCTest

- __Таймер__ — `Core/Services/PostTimerService*`
  - «До старта/До конца», прогресс 0…1, событие `reportPeriodActivityChanged`

- __Telegram__:
  - Отправка отчётов вручную и автоотправка — `Core/Services/TelegramIntegrationService.swift`, `AutoSendService.swift`
  - Обновление конфигурации — `TelegramConfigUpdater.swift`
  - Настройки UI — `Views/Settings/TelegramSettingsView.swift`
  - Настройка: [TELEGRAM_SETUP.md](TELEGRAM_SETUP.md)

- __Виджет__ — таргет `LazyBonesWidget/`
  - Быстрый статус/экшены, обновление через `WidgetCenter.reloadAllTimelines()`

- __Хранение данных__ — `Models/LocalReportService.swift`
  - Посты, статусы, теги в `UserDefaults`
  - In-memory стора при тестах (`XCTest`) + `clearPosts()` в `PostStore.init()`

- __Настройки__ — `Views/SettingsView.swift`, `Views/Settings/*.swift`
  - Уведомления (интервал, период), Telegram (токен/chat_id/автоотправка), экспорт/iCloud

- __Экспорт/iCloud__ — сервисы и тесты см. `Tests/Core/Services/ICloudServiceTests.swift`

- __Диагностика и устойчивость__
  - Логи (категории: `Logger.telegram`, уведомления/статусы)
  - Рекомендации по сбоям симулятора — [TESTING_GUIDE.md](TESTING_GUIDE.md)

- __Онбординг__ — `Views/Components/OnboardingStepView.swift`
  - Пошаговые подсказки и элементы обучения

- __Доп. UI‑компоненты__:
  - `MainActionsSectionView` — быстрые действия на главном экране
  - `ProgressBarGoodBadView` — прогресс по «Хорошее/Плохое»
  - `MercuryThermometerView` — визуализация прогресса/температуры
  - `TagPickerUIKitWheel` — колесо выбора тегов (UIKit)

## 🚀 Установка

```bash
git clone https://github.com/your-username/LazyBonesIOS.git
cd LazyBonesIOS
open LazyBones.xcodeproj
```

Соберите и запустите таргет `LazyBones` в Xcode.

## 📖 Как пользоваться

1) Откройте главное окно: создайте отчёт за сегодня или отредактируйте черновик

2) Заполните «Хорошее/Плохое», при необходимости добавьте голосовые заметки

3) Сохраните черновик или опубликуйте отчёт

4) Для Telegram‑отправки:
   - настройте Telegram в Настройках (чат, токен, автосенд — опционально)
   - отправляйте отчёты вручную из приложения или включите автоотправку

Подробнее про настройку Telegram читайте здесь:

- TELEGRAM_SETUP.md

## 🧰 Практические действия

- __Сохранить пункт как тег__
  - В формах (`LazyBones/Views/Forms/PostFormView.swift`, `DailyReportView.swift`) при вводе нового пункта, если его текста нет среди тегов, UI предлагает «Сохранить как тег».
  - Реализация: `PostFormView` добавляет через `store.addGoodTag(_:)`/`store.addBadTag(_:)` или `TagRepository` аналоги; см. `PostFormView.swift` (кнопка «Сохранить») и `DailyReportView.swift` (кнопка «Сохранить» рядом с полем ввода).
  - Старые методы стора: `PostStore.addGoodTag(_:)`, `PostStore.addBadTag(_:)` делегируют в `LocalReportService`.

- __Выбор тега и вставка в пункт__
  - В формах есть колесо/список тегов (`TagPickerUIKitWheel`). Выбор тега вставляет текст в последний пустой элемент соответствующей вкладки Good/Bad.
  - Реализация: `PostFormViewModel.addGoodTag(_:)`/`addBadTag(_:)` подставляет текст тега (с эмодзи-иконкой) в последний пустой пункт.

- __Быстрые теги__
  - В разделе тегов формы доступны быстрые кнопки с популярными тегами; нажатие мгновенно добавляет пункт.
  - Реализация: см. `PostFormView.swift` обработчики `addGoodTag`/`addBadTag` и рендер списков тегов.

- __Перенос плана в отчёт__
  - Во вкладке «План» (`LazyBones/Views/Forms/DailyPlanningFormView.swift`) создайте пункты с тегами/голосовыми и сохраните.
  - Используйте действие преобразования плана в отчёт и затем опубликуйте/отправьте в Telegram из соответствующей формы.

- __Оценка Custom‑отчётов__
  - Из списка отчётов откройте оценку для Custom карточки.
  - Реализация: `ReportsState.startEvaluation(DomainPost)` → лист `CustomReportEvaluationViewNew/Clean`, затем `completeEvaluation([Bool])` сохраняет результат.

- __Внешние и iCloud‑отчёты (read‑only)__
  - Внешние (Telegram) и iCloud отчёты не редактируются и не отправляются.
  - Доступные действия: обновить ленту, открыть группу/канал, очистить историю (см. `ExternalReportsView.swift`).
  - Сервисы: `TelegramIntegrationService.fetchExternalPosts`, `PostStore.fetchExternalPosts`, `deleteBotMessages`, `deleteAllBotMessages`.

- __Голосовые заметки__
  - В формах записывайте/прикрепляйте голосовые заметки к отчёту; при отправке в Telegram сначала уходит текст, затем последовательная отправка голосовых (см. `PostStore.publish(text:voicePaths:)`).

- __Управление тегами__
  - `TagManagerView.swift`: добавление, редактирование, удаление тегов. Категории Good/Bad переключаются сегментом.
  - Чистая архитектура: `TagRepositoryProtocol` (`save*/load*/add*/remove*/update*`), реализация — `Data/Repositories/TagRepository.swift`.

## 📘 Полное руководство: детали поведения и крайние случаи

- __Один регулярный отчёт в день__
  - Regular‑отчёт соответствует текущему дню; хранится один активный черновик/экземпляр на дату.
  - Исторические дни доступны в списке; автоотправка может догонять пропущенные дни.

- __Правила статусов__ — `Core/Services/ReportStatusManager.swift`, [STATUS_MODEL_README.md](STATUS_MODEL_README.md)
  - Старт дня → статус `.inProgress`; публикация локального отчёта → влияет на статус (например, `.sent`).
  - `forceUnlock` временно разрешает редактирование при блокирующих статусах (например, после отправки).
  - События: `Notification.Name.reportStatusDidChange` используют виджеты/уведомления для перепланирования.

- __Автоотправка__ — `Core/Services/AutoSendService.swift`, `Views/Settings/NotificationSettingsView.swift`
  - Окно выполнения: примерно 22:01–23:59 локального времени (см. логику в сервисе и тестах `Tests/AutoSendServiceTests.swift`).
  - Условия: если отчёт за день уже отправлён вручную — автоотправка пропускает; бэклог за прошлые дни отправляется при возможности.
  - Фон: выполнение зависит от iOS фоновых задач (`BackgroundTaskService`), может откладываться системой.
  - Диагностика: статус последней попытки хранится в настройках (`lastAutoSendStatus`).

- __Отправка в Telegram__ — `Core/Services/PostTelegramService.swift`, `PostStore.publish(text:voicePaths:)`
  - Порядок: сначала отправляется текстовая часть отчёта (Good/Bad), затем последовательно отправляются голосовые заметки.
  - Конфигурация берётся из настроек (`TelegramIntegrationService`, `TelegramSettingsView`). Ошибки соединения отображаются в UI.
  - Внешние отчёты: импортируются и отображаются отдельно, редактирование/отправка недоступны (`ExternalReportsView.swift`).

- __Обновление виджета__ — `LazyBonesWidget/`, `WidgetCenter.reloadAllTimelines()`
  - При значимых изменениях (публикация отчёта, смена статуса) инициируется перезагрузка таймлайнов.
  - Имя устройства для виджета настраивается в `SettingsView.deviceNameSection`.

- __Уведомления__ — `NotificationManagerService`
  - Режимы: ежечасно или дважды в день (утро/вечер). Перепланирование при изменении статусов и активности периода (`PostTimerService`).
  - Поддержка отключения побочных эффектов под тесты (см. тесты уведомлений).

- __Теги: хранение и валидация__ — `Data/Repositories/TagRepository.swift`, `Post.swift`
  - Теги разделены на Good/Bad, сохраняются в `UserDefaults`. Дубликаты в рамках категории не допускаются (`addGoodTag`, `addBadTag`).
  - «Сохранить как тег» предлагается, если введённый пункт отсутствует среди текущих тегов выбранной категории.

- __iCloud экспорт__ — `SettingsView.iCloudSection`, тесты `Tests/Core/Services/ICloudServiceTests.swift`
  - При доступности iCloud создаётся/обновляется директория `LazyBonesReports` в iCloud Drive; добавлен индикатор выполнения и результаты операции.
  - При недоступности iCloud UI подсказывает необходимые шаги: вход, включение iCloud Drive, разрешение доступа приложению.

- __Редактируемость экранов__
  - Локальные отчёты (Regular/Custom): доступны действия «Сохранить», «Опубликовать», «Отправить в Telegram».
  - External/iCloud: только просмотр, недоступны редактирование/публикация/отправка.
  - Кнопка редактирования активна при `(период активен или forceUnlock) && статус ∈ { .notStarted, .inProgress }`.

- __Модель данных__ — `Domain/Entities/DomainPost.swift`
  - `PostType`: `regular`, `custom`, `external`, `iCloud`.
  - Поля: Good/Bad пункты, теги, голосовые заметки, метаданные Telegram, поля оценки (для Custom).
  - Маппинг домен/данные: `Data/Mappers/PostMapper.swift`.

## 🗂 Подробные экраны и сценарии

- __Главный экран__ (`LazyBones/Views/MainView.swift`)
  - Отображает текущий статус дня и таймер («До старта/До конца»).
  - Быстрые действия: создать/открыть отчёт за сегодня, отправить в Telegram, открыть Настройки.
  - Учитывает `forceUnlock`: при заблокированных статусах показывает заглушку либо разрешает редактирование, если `forceUnlock == true`.

- __Список отчётов__ (`LazyBones/Views/ReportsView.swift`)
  - Разделы: локальные регулярные/кастомные, внешние (Telegram), iCloud/экспорт.
  - Режим выбора: множественное выделение, удаление, дополнительные действия.
  - Для кастомных отчётов доступна оценка через отдельный экран.

- __Ежедневный отчёт — форма__ (`LazyBones/Views/Forms/RegularReportFormView.swift`)
  - Секции «Хорошее»/«Плохое»: добавление, удаление, сортировка пунктов, фильтрация пустых.
  - Теги: быстрый выбор/создание, колесо выбора тегов (`TagPickerUIKitWheel`).
  - Голосовые заметки: запись/воспроизведение, привязка к отчёту.
  - Действия: Сохранить черновик, Опубликовать (с уведомлениями об успехе/ошибке), Отправить в Telegram.

- __Кастомный отчёт — форма__ (`LazyBones/Views/Forms/PostFormView.swift`)
  - Произвольные пункты с тегами и голосовыми заметками.
  - Оценка/пресеты состояния доступны только для Custom.
  - Действия (локальные): сохранить, опубликовать, отправить в Telegram.

- __Дневной отчёт (альтернативный UI)__ (`LazyBones/Views/Forms/DailyReportView.swift`)
  - Аналогичная структура Good/Bad с тегами и голосовыми заметками.
  - Управление состояниями формы, алерты, публикация и телеграм-отправка.

- __Планирование дня__ (`LazyBones/Views/Forms/DailyPlanningFormView.swift`)
  - Вкладки: «Отчёт» и «План». В «Плане» — список задач с тегами и голосовыми заметками.
  - Сохранение/загрузка плана, преобразование плана в отчёт и публикация/отправка в Telegram.

- __Менеджер тегов__ (`LazyBones/Views/Forms/TagManagerView.swift`)
  - Категории тегов (например, для Good/Bad), добавление/редактирование/удаление.
  - Встроенные подтверждения удаления, inline‑редактирование.

- __Внешние отчёты__ (`LazyBones/Views/Components/ExternalReportsView.swift`)
  - Получение и отображение отчётов из Telegram.
  - Обновление, переход к группе/каналу, очистка истории, обработка ошибок соединения.

- __Настройки__ (`LazyBones/Views/SettingsView.swift`, `LazyBones/Views/Settings/*.swift`)
  - Telegram, уведомления, автоотправка, данные (разблокировка/сброс), iCloud экспорт, имя устройства для виджета.

## 📝 Типы отчётов и жизненный цикл

- __Типы__ (см. `PostType`):
  - Regular — локальный ежедневный отчёт (Good/Bad, теги, голосовые)
  - Custom — локальный произвольный отчёт (поддерживает оценку)
  - External — внешний из Telegram (read‑only)
  - iCloud — импортированный из iCloud (read‑only)

- __Состояния__
  - Черновик: сохранён локально, доступно редактирование.
  - Опубликован: зафиксирован и учитывается статусной моделью (`.sent` и др.).
  - Автоотправлен: отправка выполнена сервисом автоотправки (см. настройки), приоритетно после 22:01.

- __Действия__
  - Сохранить (локальные): апдейт локального стора (см. вью‑модели форм)
  - Опубликовать (локальные): меняет статус, инициирует уведомления/виджет‑обновление
  - Отправить в Telegram (локальные): ручная отправка; автоотправка — по расписанию
  - Оценка: только для Custom

- __Потоки (flow)__
  - План → отчёт: элементы из «Плана» могут быть перенесены в локальный отчёт и опубликованы/отправлены

- __Возможности по типам (сводка)__
  - Regular (локальный ежедневный):
    - Редактирование: да (Good/Bad, теги, голосовые)
    - Публикация: да
    - Оценка: нет
    - Telegram: ручная и автоотправка
  - Custom (локальный произвольный):
    - Редактирование: да (гибкая структура, теги, голосовые)
    - Публикация: да
    - Оценка: да (см. `CustomReportEvaluationViewNew`)
    - Telegram: ручная и автоотправка
  - External (Telegram):
    - Редактирование/публикация/отправка: нет (read‑only)
    - Действия: обновить ленту, перейти в чат/канал, очистить историю
  - iCloud:
    - Редактирование/публикация/отправка: нет (read‑only)
    - Действия: экспорт локальных данных в iCloud (см. раздел «Настройки → iCloud экспорт»)

## 🏷️ Теги

- Локальные теги, группировка по категориям (Good/Bad) в UI менеджера.
- Выбор тегов в формах отчётов и планов: быстрые кнопки и колесо выбора (`TagPickerUIKitWheel`).
- Интеграция: теги отображаются рядом с пунктами отчёта и могут влиять на быстрый поиск/фильтрацию.

## 🎙 Голосовые заметки

- Запись и воспроизведение непосредственно из формы отчёта/плана.
- Привязка к конкретному пункту или к отчёту целиком (в зависимости от экрана).
- Тесты и стабильность: см. `Tests/VoiceRecorderTests.swift`.

## 📬 Интеграция с Telegram

- Ручная отправка из форм отчётов; автоотправка — по расписанию (см. «Настройки» → Автоотправка).
- Конфигурация: токен бота, `chat_id`, проверка соединения в настройках.
- Внешние отчёты: просмотр ленты, обновление, переход в Telegram, очистка истории.
- Детали настройки и ограничения API: [TELEGRAM_SETUP.md](TELEGRAM_SETUP.md).

## ⚙️ Настройки

- __Имя устройства для виджета__ (`SettingsView.deviceNameSection`):
  - Поле «Введите имя телефона» и кнопка «Сохранить имя». Имя отображается в виджете.

- __Telegram__ (`SettingsView.telegramSection`, `TelegramSettingsView`):
  - Поля: токен бота, `chat_id` группы/канала, ID бота (опционально, для фильтрации своих сообщений)
  - Кнопки: «Сохранить Telegram-данные», «Проверить связь» (показывает статус: «Успешно!»/ошибка)

- __Уведомления__ (`SettingsView.notificationSection`, `NotificationSettingsView`):
  - Переключатель «Получать уведомления»
  - Режим: «Ежечасно» или «Два раза в день (8:00 и 21:00; 21:00 — предостерегающее)»
  - Подсказка текущего расписания на сегодня (если доступно): `notificationScheduleForToday()`

- __Автоотправка отчётов__ (`SettingsView.autoSendSection`):
  - Переключатель «Включить автоотправку отчетов»
  - Примечание: автоотправка 1 раз в сутки в окно 22:01–23:59; если отправлено вручную — не отправляет; бэклог за прошлые дни догоняется; выполнение зависит от системы (фоновые задачи)
  - Статус последней попытки — `lastAutoSendStatus`

- __Данные__ (`SettingsView.dataSection`):
  - «Разблокировать отчёты» — включает `forceUnlock` для редактирования сегодняшнего отчёта без удаления данных
  - «Сбросить разблокировку отчёта» — выключает форс‑разблокировку и возвращает стандартные ограничения

- __iCloud экспорт__ (`SettingsView.iCloudSection`):
  - Если iCloud недоступен: подсказки «войти в iCloud», «включить iCloud Drive», «разрешить доступ приложению»
  - Если доступен: «Экспортировать отчеты в iCloud» (создает/обновляет папку `LazyBonesReports` в iCloud Drive), индикатор выполнения и результат
  - Кнопка «Создать тестовый файл» для проверки прав доступа

## 🔔 Уведомления и таймер

- Уведомления планируются в зависимости от статуса отчёта и активности периода
- Таймер показывает «До старта/До конца» и шлёт `reportPeriodActivityChanged`
- При статусе `.sent` всегда обратный отсчёт до следующего старта

## 🧱 Статусы и доступность действий

- Кнопка редактирования отчёта активна, если `(период активен или forceUnlock) && (status == .notStarted || .inProgress)`
- При блокирующих статусах (`.sent`, `.notCreated`, `.notSent`) показывается заглушка, если `forceUnlock == false`

Подробнее: [STATUS_MODEL_README.md](STATUS_MODEL_README.md).

## 🧪 Тестирование

См. подробное руководство: [TESTING_GUIDE.md](TESTING_GUIDE.md).

## 🧩 Устранение неполадок

- Симулятор падает с `Mach error -308` — см. раздел «Стабильность симулятора» в [TESTING_GUIDE.md](TESTING_GUIDE.md)
- Телеграм не отправляет — проверьте права бота в чате/канале и токен
- Уведомления не приходят — проверьте разрешения и интервал, перезапустите планирование

## ℹ️ Примечание

Приложение активно развивается. Документация и UX постоянно улучшаются, сохраняется обратная совместимость данных.
