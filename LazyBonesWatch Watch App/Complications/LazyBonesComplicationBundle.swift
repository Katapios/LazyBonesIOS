import WidgetKit
import SwiftUI

// @main убран - компликации должны быть в отдельном Extension target
struct LazyBonesComplicationBundle: WidgetBundle {
    var body: some Widget {
        LazyBonesComplication()
    }
}

