# 📊 Статусная модель LazyBones (вариант B)

## Базовые понятия

- Период дня:
  - Начало: start (по умолчанию 08:00, `notificationStartHour`)
  - Конец: end (по умолчанию 22:00, `notificationEndHour`)

- Таймер (`PostTimerService.updateTimeLeft()`):
  - До старта: “До старта: HH:MM:SS”
  - Во время периода: “До конца: HH:MM:SS” + прогресс 0…1
  - После конца: “До старта: HH:MM:SS” (до завтрашнего старта)
  - Важно: таймер не учитывает статус, но шлёт `.reportPeriodActivityChanged` каждый тик
  - Исключение для UI: если статус `.sent`, в любое время показывать обратный отсчет до старта следующего периода (лейбл “До старта”, прогресс 0.0)

- Статусы (`ReportStatusManager.updateStatus()`):
  - `.notStarted` — пусто и период активен
  - `.inProgress` — в локальном regular-отчёте есть черновик и период активен
  - `.sent` — отчёт отправлен
  - `.notCreated` — пусто и период не активен (до старта/после конца)
  - `.notSent` — есть записи, но период не активен (не успел отправить)
  - При `forceUnlock == true` и отчёт не отправлен — статус удерживается в разблокирующем состоянии (как минимум `.notStarted`) для доступа к редактированию; при отправке — `forceUnlock` сбрасывается

## Правила UI

- Главная кнопка (`MainViewModel.canEditReport`)
  - Включена, если `(isReportPeriodActive || forceUnlock) && (status == .notStarted || status == .inProgress)`

- Форма regular (`RegularReportFormView`)
  - Заглушка показывается, если статус блокирующий (`.sent`, `.notCreated`, `.notSent`) И `forceUnlock == false`
  - При `forceUnlock == true` заглушки нет, кнопка активна: “Создать отчёт” или “Редактировать отчёт” (если за сегодня есть черновик)

- Подписи кнопок (общее)
  - Нет записей за сегодня и можно редактировать → “Создать отчёт”
  - Есть черновик за сегодня и можно редактировать → “Редактировать отчёт”
  - Нельзя редактировать → кнопка неактивна или замещена состоянием (“Отчёт отправлен” и т.п.)

## Форс‑разблокировка

- Включение (`statusManager.unlockReportCreation()`):
  - Делает доступным создание/редактирование вне активного периода, убирает заглушки

- Сброс:
  - Автоматический — после отправки отчёта
  - Ручной — в Настройках (“Сбросить разблокировку отчёта”)
  - Важно: ручной сброс возвращает приложение и все статусы в нормальное состояние (как до нажатия “Форс‑разблокировка”)

## Кейсы (по периодам и статусам)

1) Старт активного периода (с 08:00)
- Таймер: “До конца: …”
- Статус:
  - Пусто → `.notStarted`
  - Есть черновик → `.inProgress`
  - Уже отправлен → `.sent`
- Главная:
  - `.notStarted` → кнопка активна, “Создать отчёт”
  - `.inProgress` → кнопка активна, “Редактировать отчёт”
  - `.sent` → кнопка не активна, текст “Отчёт отправлен”
- Форма regular:
  - `.notStarted`/`.inProgress` → заглушек нет
  - `.sent` → заглушка

2) Внутри активного периода (между 08:00 и 22:00)
- Таймер: “До конца: …”
- Поведение аналогично п.1:
  - `.notStarted` → “Создать отчёт”, доступно
  - `.inProgress` → “Редактировать отчёт”, доступно
  - `.sent` → недоступно, заглушка в форме

3) До старта периода (например, 07:xx)
- Таймер: “До старта: …”
- Статус:
  - Пусто → `.notCreated`
  - Есть черновик (редкий кейс) → `.notSent`
- Главная:
  - `.notCreated`/`.notSent` → кнопка выключена
- Форма regular:
  - Заглушка (если нет `forceUnlock`)

4) После конца периода (после 22:00)
- Таймер: “До старта: … (на завтра)”
- Статус:
  - Пусто → `.notCreated`
  - Есть черновик → `.notSent`
  - Отправлен → `.sent`
- Главная:
  - `.notCreated`/`.notSent`/`.sent` → кнопка выключена
- Форма regular:
  - Заглушка (если нет `forceUnlock`)

5) С включённым `forceUnlock` (в любое время)
- Проверяется статусная модель и таймеры
- Если regular‑отчёт заблокирован (по любому из кейсов) — кнопка не активна и стоит заглушка — форс‑разблокировка даёт возможность создать/отредактировать, сохранить и отправить сегодняшний regular‑отчёт (обходит статусную модель для текущего дня)
- После отправки `forceUnlock` сбрасывается

## Краткий справочник статусов и влияния

- `.notStarted`
  - Период активен, записей нет
  - Главная: кнопка активна, “Создать отчёт”
  - Форма: доступна, без заглушек

- `.inProgress`
  - Период активен, есть черновик
  - Главная: кнопка активна, “Редактировать отчёт”
  - Форма: доступна, без заглушек

- `.sent`
  - Отправлено
  - Главная: недоступно (отображение “Отчёт отправлен”)
  - Форма: заглушка

- `.notCreated`
  - Период не активен, записей нет
  - Главная: недоступно
  - Форма: заглушка (если нет `forceUnlock`)

- `.notSent`
  - Период не активен, есть записи (не успели отправить)
  - Главная: недоступно
  - Форма: заглушка (если нет `forceUnlock`)

## API менеджера статусов

- Класс: `ReportStatusManager` (`LazyBones/Core/Services/ReportStatusManager.swift`)
- Протокол: `ReportStatusManagerProtocol`
- Свойства:
  - `reportStatus: ReportStatus { get }`
  - `forceUnlock: Bool { get set }`
  - `currentDay: Date { get }`
- Методы:
  - `updateStatus()` — пересчитать статус с учётом периода/постов/forceUnlock
  - `checkForNewDay()` — обработка наступления нового дня и сбросов
  - `unlockReportCreation()` — однократная разблокировка редактирования отчёта
  - `loadStatus()` / `saveStatus()` — синхронизация с локальным хранилищем
  - `setReportStatus(_:)` — утилита для тестов/моков

### Нотификация об изменении статуса

- Имя: `Notification.Name.reportStatusDidChange`
- UserInfo: `["status": String]` — новое значение `reportStatus.rawValue`
- Высылается из `updateDependencies(_:)` при любом значимом изменении или смене `forceUnlock`
